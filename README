openssl req -nodes -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365

docker-compose:
  tls:
    build: "github.com/AnimusPEXUS/simpletlsproxy.git"
    restart: always
    volumes:
      - "./tls:/tls:z"
    command: app redmine:3000 :3000
